services:
- docker

install:
  - make image
  - docker images
  
script:
  - docker images
  - docker ps
  - bash scripts/setup.sh "scrumteamravens" "pega-ready"


before_deploy:
  - docker login -u="$DOCKER_USER" -p="$DOCKER_PASS"

deploy:
  provider: script
  script: make push

