services:
- docker

install:
  - make image
  - docker images
  
script:
  - docker images
  - docker ps
  - bash tests/pega-web-ready-testcases.yaml "$DOCKER_USER" "$REPO_NAME"


before_deploy:
  - docker login -u="$DOCKER_USER" -p="$DOCKER_PASS"

deploy:
  provider: script
  script: make push

